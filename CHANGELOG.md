# Changelog

## Unreleased

### Fixed
- Runtime `mcp({})` listing now discovers the requesting session's assigned servers instead of relying on the active project's catalog snapshot.

### Improved
- The live session status now shows the MCP server and tool being called (e.g., "Running MCP xcode/ListWindows") instead of the generic "Running mcp".
